server:
  port: 10200
netty:
  tcp-port: 10080
  serverId: gate-server
  netMessageHandlerNameSpace: com.hc.nettygame.gate.message.handler.impl
  rpc-port: 10100
  rpcServicePackage: com.hc.nettygame.gate.rpc.service.server
spring:
  config:
    import: common-config.yml
#  datasource:
#    url: jdbc:mysql://localhost:3307/mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
#    username: root
#    password: root
#    druid:
#      initial-size: 5 #连接池初始化大小
#      min-idle: 10 #最小空闲连接数
#      max-active: 20 #最大连接数
#      web-stat-filter:
#        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #不统计这些请求数据
#      stat-view-servlet: #访问监控网页的登录用户名和密码
#        login-username: druid
#        login-password: druid
#  data:
#    redis:
#      host: localhost # Redis服务器地址
#      database: 0 # Redis数据库索引（默认为0）
#      port: 6380 # Redis服务器连接端口
#      password:  # Redis服务器连接密码（默认为空）
#      timeout: 3000ms # 连接超时时间（毫秒）
#mybatis:
#  mapper-locations:
#    - classpath:dao/*.xml
#    - classpath*:com/**/mapper/*.xml

